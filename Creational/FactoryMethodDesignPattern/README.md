The Factory Pattern is a Creational Design Pattern that provides an interface for creating objects, but allows subclasses or a dedicated factory class to decide which class to instantiate.

 👉 In short: you delegate object creation to a factory instead of using new directly in your code.
